version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Pre-build phase - No commands needed for static HTML"
    build:
      commands:
        - echo "Build phase - Static HTML page, no build required"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []

